{
  "tasks": [
    {
      "id": "resume-from-checkpoint",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. Resume in dag/src/knowledge/state: given a run_id, load latest checkpoint, list nodes already NODE_COMPLETED, return next_action without redoing finished work. If workspace commit in checkpoint does not match current HEAD, resume must fail closed (do not continue blindly). Tests: completed node skipped; mismatch HEAD refused. No dag/src/loop.ts wiring yet. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(state): resume runs from checkpoints",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "wire-loop-checkpoints",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. You MAY edit dag/src/loop.ts and dag/src/task.ts for this node only; knowledge helpers stay in dag/src/knowledge/. Orchestrator still owns git push, tests, COMMIT NOW. At each existing loop phase (plan written, node start, red, green, guard/test, commit, archive, fail), append the matching checkpoint and update RunState/NodeState. Do not add UI or change TDD order. Tests in dag/src/knowledge/ cover the hook helpers used by the loop (recordPhase). Agent never git push. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(loop): write checkpoints at phase changes",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "codebase-file-symbol-index",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. Implement in dag/src/knowledge/codebase. Scan a fixture tree: files, imports, exports, symbols (exported functions/classes from ts/js). Query: where is symbol Y defined, which files import path X. Local filesystem only, no embeddings yet, no cloud. Tests use a temp directory fixture, not this whole repo. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(codebase): index files imports and symbols",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "codebase-tests-deps-index",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. Extend dag/src/knowledge/codebase: test files (*.test.ts, *.spec.ts), package.json dependencies, which tests sit next to a module path. Query: which tests cover path X, which package deps a file's directory uses. Local only. Tests with fixtures. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(codebase): index tests and package deps",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "codebase-git-index",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. In dag/src/knowledge/codebase: for a path, collect recent git log subjects and hashes via git log (local repo). Query: recent commits touching this file. Do not dump full git history to a LLM. Tests: init a temp git repo, commit a file, index returns that subject. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(codebase): index recent git history for paths",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "memory-store-jsonl",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. In dag/src/knowledge/memory: MemoryStore independent of any LLM provider: add, search, get, update, invalidate. Persist as local JSONL under .agent-memory/project (no cloud vector DB). search by substring in title/content. Tests: add, get, search, update, invalidate. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(memory): add local jsonl memory store",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "memory-types-evidence",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. In dag/src/knowledge/memory: typed entries FACT DECISION CONVENTION ARCHITECTURE FAILURE SOLUTION LESSON INVARIANT TEST_INSIGHT DEPENDENCY. Required fields: id, type, title, content, scope files/modules/symbols, evidence run_id/node_id/tests, confidence 0-1, created_at, last_verified_at, status candidate|verified|stale|invalidated. Reject add() without evidence for FACT/DECISION/ARCHITECTURE (LLM-only prose must not become verified). Tests cover reject-without-evidence and status values. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(memory): require types evidence and status",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "memory-gate-invalidate",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. In dag/src/knowledge/memory: MemoryGate persist only if factual, useful later, has evidence, has scope. invalidate(id, reason) sets status invalidated and keeps the record. markStale(id) sets stale. search() default excludes stale and invalidated. Tests: gate drops candidate without evidence; invalidate hides from default search but get() still returns it. No cloud. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(memory): add memory gate and invalidation",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    },
    {
      "id": "retrieval-exact-path-symbol",
      "prompt": "TypeScript vitest. Package root is dag/src/knowledge/ relative to the repository root. This node's tests.cwd is dag/src/knowledge. Do NOT create a top-level src/ directory at the repository root. Do NOT write knowledge files into dag/src engine modules. Hybrid retrieval phase 1 without embeddings in dag/src/knowledge/: exact match, path/module match, symbol match against MemoryStore plus codebase index. Rank by simple scores (exact > symbol > path). Tests with fixtures: query refresh token finds token.ts and a FAILURE memory scoped to that file. Use env for URLs, never hardcode, never commit .env.",
      "commit": "feat(retrieval): match exact path and symbols",
      "tests": [
        {
          "cwd": "dag/src/knowledge",
          "cmd": "yarn",
          "args": [
            "test"
          ],
          "optionalCwd": true
        }
      ],
      "allowEmptyCommit": false
    }
  ]
}
